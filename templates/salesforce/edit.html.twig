{% extends 'base.html.twig' %}
{% block title %}Редактировать отправку{% endblock %}

{% block body %}
    <div class="max-w-xl mx-auto mt-10 bg-white dark:bg-gray-800 p-6 rounded shadow">
        <h1 class="text-2xl mb-4 text-gray-800 dark:text-white">✏️ Редактировать отправку</h1>

        <form method="post">
            {% for field in ['company', 'fullName', 'phone', 'email', 'city'] %}
                <div class="mb-4">
                    <label for="{{ field }}" class="block text-gray-700 dark:text-gray-300">
                        {{ field|capitalize }}
                    </label>
                    <input type="text" name="{{ field }}" id="{{ field }}"
                           value="{{ attribute(submission, field) }}"
                           class="w-full p-2 border rounded dark:bg-gray-700 dark:text-white" required>
                </div>
            {% endfor %}

            <button class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700" type="submit">
                💾 Сохранить
            </button>
        </form>
    </div>
{% endblock %}
